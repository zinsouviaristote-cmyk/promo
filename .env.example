# URL publique du site, utilisée pour les métadonnées Open Graph et le JSON-LD.
NEXT_PUBLIC_SITE_URL=https://exemple.vercel.app

# Optionnel : URL d'un webhook (ex. Zapier/Make vers Google Sheets) qui reçoit
# chaque réservation en POST JSON. Laisser vide pour désactiver.
ORDER_WEBHOOK_URL=
